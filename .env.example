# Supabase Configuration
# Get these values from https://supabase.com/dashboard/project/YOUR_PROJECT_ID/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
# Required for npm run seed:full-test and npm run smoke:full-test.
# Prefer the Supabase Session Pooler connection string when direct db.<project>.supabase.co DNS is unavailable.
SUPABASE_DB_URL=postgresql://postgres.your-project-id:your-db-password@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres
# Optional fallback if SUPABASE_DB_URL is not set.
SUPABASE_DB_PASSWORD=your-db-password-here

# OpenAI API Configuration (Optional for AI features)
# Get your free API key from: https://platform.openai.com/api-keys
# Free tier includes $5 credit per month (sufficient for basic AI features)
# Leave empty to disable AI features (system will use rule-based fallbacks)

VITE_OPENAI_API_KEY=your_openai_api_key_here

# Application Configuration
VITE_APP_NAME=SSTH Inventory System
VITE_APP_VERSION=2.0
VITE_DEFAULT_LANGUAGE=en

# Instructions:
# 1. Copy this file to .env in the project root
# 2. Replace the values with your actual credentials
# 3. Never commit .env to git (it's in .gitignore)
#
# To get your credentials:
# 1. Supabase: Go to https://supabase.com/dashboard → Select project → Settings → API
# 2. OpenAI: Go to https://platform.openai.com/api-keys → Create new secret key
# 3. AI features will work with limited functionality without OpenAI API key
